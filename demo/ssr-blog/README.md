# SSR Blog Demo

Demonstrates a `[slug]` blog route in `hydrated` mode with server-side data loading, bootstrap payload rendering, compiler-loaded filesystem routes, and route components authored against `van-stack/render`.
