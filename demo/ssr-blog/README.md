# SSR Blog Demo

Demonstrates a `[slug]` blog route in `hydrated` mode with server-side data loading, bootstrap payload rendering, routes imported from `.van-stack/routes.generated.ts`, and route components authored against `van-stack/render`.
