# SSG Demo

Demonstrates static generation from route entries discovered under `src/routes`, loaded through the compiler at runtime, and rendered through `van-stack/render`.

For the quickest evaluator flow, start with `bun run start` and `demo/showcase`. This folder stays as the focused reference for the SSG path after the five-mode showcase.
